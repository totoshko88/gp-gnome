# Як перезавантажити Extension на Wayland

## ✅ Файли оновлено

Extension файли успішно оновлено в системі:
- Шлях: `~/.local/share/gnome-shell/extensions/globalprotect@username.github.io/`
- Час оновлення: 16:10
- Статус: ACTIVE

## ✅ Нові функції встановлено

Перевірено наявність нового коду:
- ✅ Host State menu item
- ✅ CSS padding-left: 12px для Advanced menu
- ✅ getConnectionDetails() метод

## 🔄 Як перезавантажити Extension

### На Wayland (ваша система):

**Єдиний надійний спосіб** - вийти з сесії та увійти знову:

1. Збережіть всю роботу
2. Натисніть на меню в правому верхньому куті
3. Виберіть "Вийти" або "Log Out"
4. Увійдіть знову

### Альтернатива (може не спрацювати на Wayland):

```bash
# Спробувати перезапустити extension
gnome-extensions disable globalprotect@username.github.io
sleep 2
gnome-extensions enable globalprotect@username.github.io
```

Але це **не гарантує** перезавантаження коду на Wayland.

## 📋 Після перезавантаження

Перевірте що extension працює з новими функціями:

1. **Відкрийте меню extension** (клік на іконку в панелі)
2. **Перевірте структуру меню**:
   - Not connected / Connected (з Gateway info)
   - Connect / Disconnect
   - Advanced (має бути вирівняне)
   - Show (має містити Host State)
   - Settings
   - Select Gateway

3. **Підключіться до VPN**
4. **Перевірте статус** - має показувати:
   ```
   Connected to portal.example.com
   Gateway: vpn-xx.epam.com
   Assigned IP: 100.64.x.x
   Gateway IP: xxx.xxx.xxx.xxx
   ```

5. **Перевірте Select Gateway**:
   - Відкрийте Select Gateway
   - Виберіть інший gateway
   - Перевірте що підключення відбулось до ОБРАНОГО gateway

6. **Перевірте Show → Host State**:
   - Має показати HIP інформацію

7. **Перевірте Advanced → Collect Logs**:
   - Має відкрити теку з логами

## 🐛 Якщо щось не працює

Перегляньте логи:
```bash
journalctl -f -o cat /usr/bin/gnome-shell | grep -i globalprotect
```

Або перевірте Looking Glass (Alt+F2 → `lg`):
- Перейдіть на вкладку "Extensions"
- Знайдіть globalprotect@username.github.io
- Перевірте чи є помилки

## 📝 Примітка

GNOME Shell на Wayland кешує JavaScript код extensions і не перезавантажує його при disable/enable. Тому **обов'язково потрібен вихід/вхід** для завантаження нових файлів.
