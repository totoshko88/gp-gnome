# Extension потребує перезапуску GNOME Shell

## Проблема

Extension не запустився після останнього оновлення. Логи показують:
```
Schema org.gnome.shell.extensions.globalprotect could not be found
```

## Причина

При попередньому `make install` тека `schemas/` не була скопійована правильно.

## Виправлення

1. ✅ Виконано `make uninstall`
2. ✅ Виконано `make install`
3. ✅ Schemas тепер на місці:
   ```
   ~/.local/share/gnome-shell/extensions/globalprotect@username.github.io/schemas/
   ├── gschemas.compiled
   └── org.gnome.shell.extensions.globalprotect.gschema.xml
   ```

## Перевірка

```bash
# Extension встановлено
ls ~/.local/share/gnome-shell/extensions/globalprotect@username.github.io/

# Schemas на місці
ls ~/.local/share/gnome-shell/extensions/globalprotect@username.github.io/schemas/

# Код без синтаксичних помилок
# getDiagnostics: No diagnostics found
```

## Наступні кроки

**ОБОВ'ЯЗКОВО потрібен перезапуск GNOME Shell**:

### На Wayland (ваша система):
1. Збережіть всю роботу
2. Вийдіть з сесії (Log Out)
3. Увійдіть знову

### Після перезапуску:

```bash
# Перевірити що extension з'явився в списку
gnome-extensions list | grep globalprotect

# Увімкнути extension
gnome-extensions enable globalprotect@username.github.io

# Перевірити статус
gnome-extensions info globalprotect@username.github.io
```

## Чому потрібен перезапуск?

1. **GNOME Shell кешує список extensions** при старті
2. **Schemas компілюються** при завантаженні Shell
3. **На Wayland** не можна перезапустити Shell без виходу з сесії
4. **Нові файли** не підхоплюються без перезапуску

## Що було виправлено в цій версії

1. ✅ Уніфіковано всі Show діалоги (Host State, HIP State, Errors, Notifications, Help, Version)
2. ✅ Всі Show пункти тепер відкривають модальні вікна зі скролінгом
3. ✅ Schemas правильно встановлені

## Очікуваний результат

Після перезапуску GNOME Shell:
- Extension з'явиться в списку
- Можна буде увімкнути через `gnome-extensions enable`
- Іконка з'явиться в панелі
- Всі Show пункти відкриватимуть діалоги замість notifications

## Статус

**Код**: ✅ Без помилок  
**Schemas**: ✅ Встановлені  
**Extension**: ✅ Встановлений  
**Потрібно**: 🔄 Перезапуск GNOME Shell (вихід/вхід)
