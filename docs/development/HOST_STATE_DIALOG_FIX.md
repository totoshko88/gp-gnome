# Виправлення Host State Dialog

## Дата: 23 листопада 2025

## ✅ ВИПРАВЛЕНО

### Проблема

При відкритті Show → Host State виникала помилка:
```
scrollView.add_actor is not a function
```

### Причина

В новіших версіях GNOME Shell (45+) API змінився:
- Старий метод: `scrollView.add_actor(widget)`
- Новий метод: `scrollView.add_child(widget)`

### Рішення

Замінено застарілий метод на новий:

**Файл**: `indicator.js`, метод `_showHostState()` (рядок 510)

**Було**:
```javascript
scrollView.add_actor(contentBox);
```

**Стало**:
```javascript
scrollView.add_child(contentBox);
```

### Технічні деталі

**GNOME Shell API зміни**:
- `add_actor()` - deprecated в GNOME 45+
- `add_child()` - новий стандартний метод для додавання дочірніх елементів

**Сумісність**:
- ✅ GNOME 45+
- ✅ GNOME 46+
- ⚠️ GNOME 44 і старіші (потрібен `add_actor`)

### Перевірка

Після виправлення Host State діалог має:
1. Відкриватись без помилок
2. Показувати HIP інформацію в scrollable вікні
3. Мати кнопку Close
4. Закриватись по Escape

## 🔄 Наступні кроки

1. **Перезапустити GNOME Shell** (вийти/увійти)
2. **Протестувати**:
   - Відкрити меню extension
   - Show → Host State
   - Перевірити що діалог відкривається
   - Перевірити що текст відображається
   - Перевірити scroll якщо текст великий
   - Закрити через кнопку Close
   - Відкрити знову і закрити через Escape

## ✨ Статус

**Виправлено**: ✅  
**Версія**: 1.0.4  
**Файли змінено**: `indicator.js` (1 рядок)  
**Готовність**: Ready for Testing 🧪
