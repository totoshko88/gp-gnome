# TODO для наступної сесії

## Дата: 23 листопада 2025

## Статус поточної сесії: ✅ Завершено

### Що зроблено в цій сесії:

1. ✅ Виправлено всі критичні баги
2. ✅ Додано інтерактивні Settings діалоги
3. ✅ Додано Copy функцію для Show діалогів
4. ✅ Спрощено Settings меню (один пункт замість submenu)
5. ✅ Додано опис extension в metadata.json, prefs.js, Show → Version
6. ✅ Підготовлено schema для username
7. ✅ Додано "Report Issue" в Advanced меню
8. ✅ Створено детальний план реалізації

---

## TODO для наступної сесії:

### 1. Username функціонал (30 хв)

#### Файли для зміни:
- `indicator.js` - додати username поле в `_openSettingsDialog()`
- `gpClient.js` - змінити `connect()` для підтримки username параметру
- `indicator.js` - передавати username в `connect()`

#### Готовий код в:
`FINAL_IMPLEMENTATION_PLAN.md` - секція "1. Username в Settings"

---

### 2. Report Issue функціонал (15 хв)

#### Файли для зміни:
- `gpClient.js` - додати метод `reportIssue()`
- `indicator.js` - додати метод `_reportIssue()`

#### Готовий код в:
`FINAL_IMPLEMENTATION_PLAN.md` - секція "2. Report Issue в Advanced"

---

### 3. GPL License (50 хв)

#### Файли для створення/зміни:
- `LICENSE` - створити GPL-3.0 текст
- `metadata.json` - додати license поля
- `README.md` - додати License секцію
- Всі .js файли (10 файлів) - додати GPL header

#### Готовий код в:
`FINAL_IMPLEMENTATION_PLAN.md` - секція "3. GPL License"

---

## Порядок виконання:

### Крок 1: Username (найпростіше)
1. Відкрити `indicator.js`
2. Знайти метод `_openSettingsDialog()`
3. Додати username поле після Poll Interval
4. Додати збереження username в Save button
5. Відкрити `gpClient.js`
6. Змінити метод `connect()` - додати параметр username
7. Додати `--username` в args якщо username вказано
8. Відкрити `indicator.js`
9. Знайти метод `_onToggleConnection()`
10. Отримати username з settings
11. Передати username в `connect()`

### Крок 2: Report Issue
1. Відкрити `gpClient.js`
2. Додати метод `reportIssue()` після `getNotifications()`
3. Відкрити `indicator.js`
4. Додати метод `_reportIssue()` після `_showNotifications()`

### Крок 3: GPL License
1. Створити `LICENSE` файл з GPL-3.0 текстом
2. Відкрити `metadata.json` - додати license поля
3. Відкрити `README.md` - додати License секцію
4. Додати GPL header в кожен .js файл:
   - extension.js
   - indicator.js
   - gpClient.js
   - statusMonitor.js
   - errorHandler.js
   - prefs.js

### Крок 4: Тестування
1. `make uninstall`
2. `make install`
3. Перезапустити GNOME Shell (вийти/увійти)
4. Протестувати Username в Settings
5. Протестувати Report Issue в Advanced
6. Перевірити що extension працює

---

## Важливі файли:

### Документація:
- `FINAL_IMPLEMENTATION_PLAN.md` - детальний план з готовим кодом
- `ADVANCED_FEATURES_TODO.md` - список всіх запитаних функцій

### Змінені файли:
- `schemas/org.gnome.shell.extensions.globalprotect.gschema.xml` - додано username key
- `indicator.js` - додано Report Issue пункт в меню

---

## Оцінка часу:

- Username: 30 хв
- Report Issue: 15 хв
- GPL License: 50 хв
- Тестування: 15 хв

**Загалом**: ~2 години

---

## Примітки:

1. Всі фрагменти коду готові в `FINAL_IMPLEMENTATION_PLAN.md`
2. Schema вже оновлена (username додано)
3. Report Issue пункт вже в меню
4. Потрібно тільки додати реалізацію методів

---

## Поточний стан extension:

### Версія: 1.0.11
### Статус: Готовий до тестування (після перезапуску Shell)

### Основні функції:
- ✅ Connect/Disconnect з MFA
- ✅ Gateway selection з кешуванням
- ✅ Інтерактивні Settings діалоги
- ✅ Show діалоги з Copy кнопкою
- ✅ Advanced операції (Rediscover, HIP, Logs)
- ✅ Auto-disconnect при logout
- ✅ Retry логіка для CLI багів
- ⏳ Username підтримка (готово до реалізації)
- ⏳ Report Issue (готово до реалізації)

### Що залишилось:
- Username функціонал
- Report Issue функціонал
- GPL License

---

## Команди для наступної сесії:

```bash
# Перевірити поточний стан
gnome-extensions info globalprotect@username.github.io

# Після змін
make uninstall
make install
gnome-extensions enable globalprotect@username.github.io

# Перезапустити Shell (Wayland)
# Вийти з сесії та увійти знову

# Перевірити логи
journalctl -f -o cat /usr/bin/gnome-shell | grep -i globalprotect
```

---

## Контакти:

**Автор**: Anton Isaiev  
**Email**: totoshko88@gmail.com  
**Repository**: https://github.com/totoshko88/gp-gnome

---

## Дякую за роботу! 🚀

Extension майже готовий. Залишилось додати останні функції та GPL ліцензію.
