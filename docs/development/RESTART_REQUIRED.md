# Потрібен перезапуск GNOME Shell

## 🔄 Ситуація

Extension в стані ERROR після виправлення синтаксичної помилки.

**Проблема:** Дублікат `const result` було виправлено, але GNOME Shell закешував стару версію з помилкою.

## ✅ Що зроблено

1. Виправлено синтаксичну помилку (видалено дублікат `const result`)
2. Видалено extension повністю
3. Встановлено заново
4. Увімкнено extension

## ⚠️ Що потрібно зробити

### На Wayland (ваша поточна сесія):

**Потрібен вихід з сесії та вхід знову:**

1. Збережіть всю роботу
2. Натисніть на меню в правому верхньому куті
3. Виберіть "Вийти" або "Log Out"
4. Увійдіть знову

### Після входу:

```bash
# Перевірте статус
gnome-extensions info globalprotect@username.github.io | grep -E "Стан|State"

# Повинно показати:
# Стан: ACTIVE

# Якщо все ще ERROR, увімкніть знову:
gnome-extensions disable globalprotect@username.github.io
gnome-extensions enable globalprotect@username.github.io
```

## 🔍 Перевірка після перезапуску

### 1. Статус extension:
```bash
gnome-extensions info globalprotect@username.github.io
```

Повинно показати:
- Увімкнено: Так
- Стан: ACTIVE ✅

### 2. Логи:
```bash
journalctl -b 0 /usr/bin/gnome-shell --since "1 minute ago" --no-pager | grep -i globalprotect
```

Повинно показати:
```
GlobalProtect Extension: enable() called
GlobalProtect Extension: settings loaded
GlobalProtect Extension: client created
GlobalProtect Extension: status monitor created
GlobalProtect Extension: indicator created
GlobalProtect Extension: indicator added to panel
GlobalProtect Extension: status monitoring started
```

### 3. Іконка в панелі:
- Повинна з'явитись іконка GlobalProtect в системному треї
- Клікніть на неї - повинно відкритись меню

## 📝 Що було виправлено

### Синтаксична помилка:

**Було (помилка):**
```javascript
const result = await this._executeCommand(...);

// ... retry логіка ...

const result = await this._executeCommand(...); // ❌ Дублікат!
```

**Стало (виправлено):**
```javascript
const result = await this._executeCommand(...);

// ... retry логіка ...

// Використовуємо той самий result ✅
```

## 🚀 Після перезапуску

Extension повинен працювати з усіма виправленнями:
- ✅ Retry логіка для "already established" помилки
- ✅ Виправлені команди disconnect, collectLog, getDetails
- ✅ Перемикання gateway з disconnect → set → connect
- ✅ Всі меню реорганізовані
- ✅ 21 функція доступна

## 💡 Альтернатива (якщо не хочете виходити зараз)

Можна продовжити роботу і перезапустити пізніше. Extension буде працювати після наступного входу в систему.

**Примітка:** На Wayland немає способу перезапустити GNOME Shell без виходу з сесії (на відміну від X11 де можна Alt+F2 → r).
